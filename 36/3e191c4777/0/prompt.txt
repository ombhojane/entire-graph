Read ATC_PLAN.md and BUILDATHON.md. Verify the build: run tools/atc-fixture/build_fixture.sh /tmp/atc-fixture, then python3 tools/atc/collide.py feat-auth feat-checkout --repo /tmp/atc-fixture (expect HOLD, exit 2) and python3 tools/atc/collide.py feat-logging feat-docs --repo /tmp/atc-fixture (expect CLEARED, exit 0). Then append a "Decisions log (pre-noon)" section to BUILDATHON.md recording: chosen — Track 2 blast-radius intersection; rejected — Track 3 framing (category mismatch), heal-agents idea (harness auto-update trend), red-severity for body-only changes (false-positive risk); assumptions — tree-sitter resolution is sufficient for RW detection, pairwise refs before --all; open risks as listed. Commit as the pre-noon stable state and push origin buildathon.